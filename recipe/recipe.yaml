context:
  name: keygen-py
  version: "0.2.1"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/keygen_py-${{ version }}.tar.gz
  sha256: 330b6afc62a25ca87c8d76895bda7f32482eb1857dde36c2f19c1cac479030ca

build:
  number: 0
  skip: match(python, "<3.11") or match(python, ">3.14")

requirements:
  build:
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - maturin
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - ${{ compiler('rust') }}
    - make
    - perl
    - cargo-bundle-licenses
  host:
    - python
    - pip
    - setuptools
    - wheel
    - maturin
  run:
    - python

tests:
  - python:
      imports:
        - keygen_sh
      pip_check: true

about:
  summary: Unofficial Keygen SDK for Python. Integrate license activation and offline licensing. Wrapper around keygen-rs rust crate
  license: MIT
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  homepage: https://github.com/terra-quantum-public/keygen-py

extra:
  recipe-maintainers:
    - sbtq
    - cedrictq
